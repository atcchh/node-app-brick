process.umask(077);

var reStore = require('restore'),
    store   = new reStore.FileTree({path: '/home/caoyanfei079/.storage/'}),

    server  = new reStore({
                store:  store,
                http:   {port: 8000},
                allow: {
                	signup: true
		}
              });

server.boot();
