//url-test.js
var url = require("url");
var urlString = [
"//www.lufax.com/test",
"http://www.lufax.com/test",
]
for(var i =0; i < urlString.length;i++) {
	var tempUrl = urlString[i];
	console.log(url.parse(tempUrl));
}